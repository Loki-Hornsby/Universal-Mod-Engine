using System;
using System.IO;

using Software;

namespace Interface {
    /*
    public class CustomMessage {
        public int level;
        public string message;

        public CustomMessage(int l, string m){
            level = l;
            message = m;
        }
    }

    public static class InterfaceLoader {
        /// <summary>
        /// Get the mods for a single interface
        /// </summary>
        static FileInfo GetMods(){
            string Path = "./Mods/";
        }

        /// <summary>
        /// Get the interfaces and load them
        /// </summary>
        static string[] GetInterfaces(){
            string CurrentPath = System.IO.Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);
            string[] fileArray = Directory.GetFiles(CurrentPath, "Interface.cs", SearchOption.AllDirectories);
        }

        public static int Main(string[] args){
            string[] interfaces = GetInterfaces();
            foreach (string item in interfaces) console.WriteLine(item);

            return 0;
        }
    }*/
}